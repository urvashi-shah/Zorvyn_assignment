services:
  - type: web
    name: finguard-api
    env: java
    rootDir: finance-api
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/finguard-api-0.0.1-SNAPSHOT.jar --server.port=$PORT
