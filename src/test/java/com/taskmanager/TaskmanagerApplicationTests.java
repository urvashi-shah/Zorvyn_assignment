package com.taskmanager;

import org.junit.jupiter.api.Test;

class TaskmanagerApplicationTests {

    @Test
    void contextLoads() {
        // Intentionally empty: we avoid starting the full Spring context in tests
        // so the project builds even before MongoDB/JWT secrets are configured.
    }
}

